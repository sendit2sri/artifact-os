{
  "files": [
    "tools/devloop/main.py",
    "tools/devloop/utils/patch_validate.py"
  ],
  "steps": [
    "Update import paths to match new monorepo structure",
    "Refine file filtering logic in main function",
    "Add error handling for non-existent files",
    "Extract diff body from unified diff text"
  ],
  "notes": "Ensure all imports are correctly placed and update file path checks."
}